<% @page_title = "New music label" %>
<%= link_to("back to index" , music_company_labels_path(@label))%>
<%= error_messages_for(@label) %>


<%= form_for([@company,@label]) do |f| %>

	<%= render(:partial => "labels/form" , :locals => {:f => f}) %>
	<%= submit_tag("Create music label") %>
<% end %>