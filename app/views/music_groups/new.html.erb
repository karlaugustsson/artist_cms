<% @page_title = "New music_group" %>
<%= link_to("back to index" , artist_music_groups_path(@artist))%>
<%= error_messages_for(@group) %>


<%= form_for([@artist,@group]) do |f| %>

	<%= render(:partial => "music_groups/form" , :locals => {:f => f}) %>
	<%= submit_tag("Create music Group", :class => "btn btn-primary") %>
<% end %>