<% @page_title = "New music comapny account" %>
<%= error_messages_for(@company) %>


<%= form_for(@company) do |f| %>

	<%= render(:partial => "music_companies/form" , :locals => {:f => f}) %>
	<%= submit_tag("Create music company account") %>
<% end %>
