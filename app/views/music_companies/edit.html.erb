<% @page_title = "edit music company account" %>
<%= error_messages_for(@company) %>


<%= form_for(@company) do |f| %>

	<%= render(:partial => "music_companies/form" , :locals => {:f => f}) %>
	<%= submit_tag("update music company account") %>
<% end %>