

<%= f.label(:name) %>

<%= f.text_field(:name) %>

<%= f.label(:release_date) %>

<%= f.date_field(:release_date) %>

<%= f.label(:album_art) %>

<%= f.file_field(:album_art) %>

<%= f.hidden_field(:album_art_cache) %>

<%= f.label(:published) %>

<%= f.check_box(:published) %>


  <% for label in @label_ids %>
		 <%= f.label("#{:label_ids}_#{label.id}",label.label_name) %>
      <%=f.check_box(:label_ids,{ :multiple => true }, label.id, nil)  %>
     

  <% end %>


