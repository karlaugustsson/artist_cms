<% @page_title = "edit artist" %>
<%= link_to("back to music groups" , artist_path(@artist))%>
<%= error_messages_for(@artist) %>


<%= form_for(@artist) do |f| %>

	<%= render(:partial => "artists/form" , :locals => {:f => f}) %>
	<%= submit_tag("update artist", :class=> "btn btn-primary") %>
<% end %>