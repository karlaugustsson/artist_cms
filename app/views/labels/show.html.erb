<% @page_title = "My Label" %>
<%= link_to("back to all labels" , music_company_labels_path(@company))%>
<h2><%= @label.label_name %></h2>
	<%= link_to("edit my music label" , edit_music_company_label_path(@company,@label))%>

	<%= link_to("delete my music label" , delete_music_company_label_path(@company,@label))%>


	


