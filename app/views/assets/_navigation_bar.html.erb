<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#toggleMenu">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a class="navbar-brand" href='/'> Artist cms</a>
      </div>
      <div id="toggleMenu" class="collapse">
    
      <ul class="nav navbar-nav navbar-right">
           
        
          <%if @onlineArtist || @onlineMusicCompany %>
                <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#">Menu<span class="caret"></span></a>
      <ul class="dropdown-menu">
              <li>
                <%= link_to(!@onlineMusicCompany.blank? ? music_company_path(@onlineMusicCompany.id) : artist_path(@onlineArtist.id)) do %>
                   <span class="glyphicon glyphicon-user"></span>Profile
          
                <%end%>
              </li>

                  <li>
                <%= link_to(!@onlineMusicCompany.blank? ? music_company_settings_path(@onlineMusicCompany.id) : artist_settings_path(@onlineArtist.id)) do %>
                   <span class="glyphicon glyphicon-user"></span>Settings
          
                <%end%>
              </li>

              <% if @onlineMusicCompany && @onlineMusicCompany.super_power == 1 %>
              <li>

                <%= link_to (music_companies_path()) do %>
                  <span class="glyphicon glyphicon-th-list"></span>list accounts
                <%end%>
              </li> 
              <% end %>
             <li>
              <%= link_to (!@onlineMusicCompany.blank?  ? "/music_company_logout"  : {:action => "logout" ,:controller => "login_artist"} ) do %>
              <span class="glyphicon glyphicon-log-in"></span> Logout
              <%end%>
              </li> 

         <%else%>
            <li>
              <%= link_to (new_artist_path ) do %>
              <span class="glyphicon glyphicon-user"></span>Register Artist
              <%end%>
              </li>
              <li>
              <%= link_to ({:action => "login" ,:controller => "login_artist"}) do %>
              <span class="glyphicon glyphicon-log-in"></span> Login
              <%end%>
              </li> 
        <%end%>
              </ul>
    </li>
      </ul>
      </div>
    
  </div>
</nav>